# Calculate-estimation-pricing capture

The current UI Calculate Price action completed and the current session readback was `GetEstimatePricingTotalJsonResponse` (total $2,771.90). The complete `POST /api/services/app/Estimate/CalculateEstimationPricing` request/response for the same lead/estimate and pricing baseline is included here from the sanitized Flow 02 HAR because that endpoint did not appear in this session’s resource list.

Authentication headers and cookies intentionally redacted.
