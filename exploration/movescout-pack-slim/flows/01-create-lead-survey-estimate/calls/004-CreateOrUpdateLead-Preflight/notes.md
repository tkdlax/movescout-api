Preflight response had no body (stored as JSON null). This is browser-generated CORS traffic, not an application mutation.
The preflight Access-Control-Request-Headers included `authorization`, `cache-control`, `content-type`, `expires`, `pragma`, and `x-requested-with`; no application request body.
